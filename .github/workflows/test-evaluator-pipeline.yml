name: test-evaluator-pipeline
on:
  push:
    branches: [main, develop]
  pull_request:
jobs:
  run:
    uses: ./.github/workflows/shared-setup.yml
    with:
      run: npm run evaluator-pipeline-check
