name: test-stack-lint
on:
  push:
    branches: [main, develop]
  pull_request:
jobs:
  run:
    uses: ./.github/workflows/shared-setup.yml
    with:
      run: npm run lint-stacks && npm run pair-names
