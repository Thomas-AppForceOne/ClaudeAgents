/**
 * Library entry point for `@claudeagents/config-server`.
 *
 * Re-exports grow over the R1 sprint sequence (S2: read paths; S5: stack
 * resolution; S7: validation surface). Sprint 1 ships an empty shell so
 * downstream packages can take a typed dependency now.
 */

export {};
