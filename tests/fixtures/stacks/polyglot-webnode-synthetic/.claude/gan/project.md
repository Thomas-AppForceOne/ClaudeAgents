---
schemaVersion: 1
---

# Project overlay (polyglot fixture)

Empty splice points — the polyglot fixture exercises auto-detection across
both stacks; the project overlay carries only the required `schemaVersion`.
Adding splice points here would shift the test focus away from the
multi-stack guard rail.
