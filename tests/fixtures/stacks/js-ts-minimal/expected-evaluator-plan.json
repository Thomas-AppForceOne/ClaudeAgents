{
  "activeStacks": [
    {
      "name": "generic",
      "scope": [
        "**/*"
      ]
    }
  ],
  "auditCommands": [],
  "buildTestLint": {},
  "docLintInvocations": [],
  "documentationSurfacesInstantiated": [
    {
      "appliesToFiles": [
        "src/handler.ts",
        "src/util.ts"
      ],
      "id": "comments_cite_no_development_provenance",
      "stack": "generic",
      "templateText": "Every comment, and every user-facing string the code emits (an error message, a log line, text shown to a user), added in this sprint explains the code's reasons — a constraint, an invariant, a non-obvious decision and its rationale — and references no development-process artifact: a specification or RFC identifier used as a process tag, a sprint or iteration label, a ticket, or a pull-request number. It says why the code is the way it is, not when, by which process, or under which plan it was written. A citation that is itself the rationale (an upstream bug the code works around, a standard the code implements) is allowed.\n",
      "triggerEvidence": {
        "keywordsHit": [],
        "scopeMatched": [
          "src/handler.ts",
          "src/util.ts"
        ]
      }
    },
    {
      "appliesToFiles": [
        "src/handler.ts",
        "src/util.ts"
      ],
      "id": "comments_explain_why_not_what",
      "stack": "generic",
      "templateText": "Every comment added in this sprint explains a constraint, an invariant, or a non-obvious decision and its rationale — not a restatement of what the adjacent code already expresses. No added comment can be deleted without losing information absent from the code itself.\n",
      "triggerEvidence": {
        "keywordsHit": [],
        "scopeMatched": [
          "src/handler.ts",
          "src/util.ts"
        ]
      }
    },
    {
      "appliesToFiles": [
        "src/handler.ts",
        "src/util.ts"
      ],
      "id": "nonobvious_decision_cites_rationale",
      "stack": "generic",
      "templateText": "Any non-obvious implementation decision in changed code (a workaround, a performance trade-off, an ordering constraint) cites its rationale in a comment or doc contract.\n",
      "triggerEvidence": {
        "keywordsHit": [],
        "scopeMatched": [
          "src/handler.ts",
          "src/util.ts"
        ]
      }
    },
    {
      "appliesToFiles": [
        "src/handler.ts",
        "src/util.ts"
      ],
      "id": "public_contract_completeness",
      "stack": "generic",
      "templateText": "Every public function, type, or interface added or changed in this sprint documents, in its doc comment, each parameter's meaning (not merely its type), the failure modes it can raise, any side effect beyond its return value, and any invariant the caller must uphold.\n",
      "triggerEvidence": {
        "keywordsHit": [],
        "scopeMatched": [
          "src/handler.ts",
          "src/util.ts"
        ]
      }
    }
  ],
  "evaluatorAdditionalChecks": [],
  "secretsScans": [
    {
      "extension": "env",
      "files": [],
      "stack": "generic"
    }
  ],
  "securitySurfacesInstantiated": []
}
