# generic-fallback fixture

A project with no recognised ecosystem signal: no `package.json`, no
lockfile, no language manifest. The evaluator-pipeline-check harness
expects this fixture to activate only the `generic` stack.
